## Scala sample logging to Azure application insights

Sample is provisioned using Terraform on Azure infrastructure, 
and logs both warn and debug logs every second.

Terraform 0.12+ is required.

```$xslt
terraform init
terraform apply
```
